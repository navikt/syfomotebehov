info:
  name: motebehov
  type: http
  seq: 1

http:
  method: GET
  url: "{{veilederBasePath}}/motebehov"
  headers:
    - name: Nav-Personident
      value: "{{personident}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
