info:
  name: behandle
  type: http
  seq: 3

http:
  method: POST
  url: "{{veilederBasePath}}/motebehov/behandle"
  headers:
    - name: Nav-Personident
      value: "{{personident}}"
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
