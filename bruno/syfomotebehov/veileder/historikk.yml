info:
  name: historikk
  type: http
  seq: 2

http:
  method: GET
  url: "{{veilederBasePath}}/historikk"
  headers:
    - name: Nav-Personident
      value: "{{personident}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
