info:
  name: arbeidstaker
  type: folder
  seq: 1

request:
  auth: inherit
  variables:
    - name: v4basePath
      value: "{{basePath}}/api/v4/arbeidstaker"
    - name: formIdentifier
      value: "motebehov-arbeidstaker-meld"
  scripts:
    - type: before-request
      code: |-
        const tokenResponse = await bru.runRequest("/mock-auth/tokenx");
        bru.setEnvVar("token", tokenResponse.data)
