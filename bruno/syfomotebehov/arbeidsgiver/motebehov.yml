info:
  name: motebehov
  type: http
  seq: 1

http:
  method: GET
  url: "{{arbeidsgiverBasePath}}/motebehov?fnr={{arbeidstakerFnr}}&virksomhetsnummer={{virksomhetsnummer}}"
  params:
    - name: fnr
      value: "{{arbeidstakerFnr}}"
      type: query
    - name: virksomhetsnummer
      value: "{{virksomhetsnummer}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
