info:
  name: tokenx
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8811/syfomotebehov/local/mocktoken/tokenx?fnr={{tokenFnr}}&clientId={{clientId}}
  params:
    - name: fnr
      value: "{{tokenFnr}}"
      type: query
    - name: clientId
      value: "{{clientId}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
